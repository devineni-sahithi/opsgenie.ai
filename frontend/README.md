# OpsGenie.AI Frontend

React + Vite dashboard showcasing incidents, AI assistant responses, and SRE operations context.

## Setup

```bash
npm install
npm run dev
```

Set `VITE_API_URL` to point to the FastAPI backend when running locally or in production.
